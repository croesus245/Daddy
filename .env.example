# Environment variables for email configuration
# Copy this file to .env.local and fill in your SMTP credentials

# Email SMTP Configuration
# For Gmail: Create App Password at https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Recipient email (where contact form emails are sent)
RECIPIENT_EMAIL=ayindesemiu@yahoo.com
